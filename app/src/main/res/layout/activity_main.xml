<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/whiteGrey"
    tools:context="com.shipwebsource.mywebsource.MainActivity">

    <include layout="@layout/toolbar"
        android:id="@+id/toolbar" />

    <android.support.design.widget.TabLayout
        android:id="@+id/tabs"
        style="@style/MyCustomTabLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/colorPrimary"
        android:minHeight="?attr/actionBarSize"
        android:layout_below="@id/toolbar"
        />

    <TextView
        android:id="@+id/titleLoggedInUser"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/tabs"
        android:layout_marginTop="5dp"
        android:layout_marginBottom="5dp"
        android:text="John Doe (WEB 12345)"
        android:textSize="12sp"
        android:layout_marginRight="10dp"
        android:textColor="@color/primaryTextColor"
        android:layout_alignParentRight="true"
        android:drawableLeft="@drawable/ic_verified_user_green_a400_24dp"
        android:drawablePadding="5dp"
        android:gravity="center_vertical"
        android:fontFamily="sans-serif-regular"/>



    <!-- View pager to swipe views -->
    <android.support.v4.view.ViewPager
        android:id="@+id/pager"
        android:layout_below="@id/titleLoggedInUser"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"/>



</RelativeLayout>
